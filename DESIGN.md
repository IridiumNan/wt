# water-repo design

## 设计初衷

- 之前用的deepseek写的仓库管理， 需要手动复制url才能在服务器上下载， 对于我这种重度键盘用户是很难受的
- 并且我发现对于我存储一些相对静态而且已经打包之后的压缩包或者镜像和配置来说， 根本不需要目录结构， 这是冗余的东西
- 我还想过给团队里面的其他人开放仓库， 但是却不能明确大家的权限， 虽然可以使用企业级的管理工具， 但是复杂的acl或者角色管理对于小型团队来说也是没有必要的
- 最近我刚在学 golang, 所以用 go 实现了这个项目

## Tag

- 我发现我之前用文件夹结构来管理仓库的时候， 也没有创建非常复杂的结构， 而只有一个不嵌套的文件夹， 所以我希望采用一种扁平化的分类管理方式， 我们只需要知道这个包属于临时分享 (temp 标签), 还是长期有效 (static 标签) 或者 是属于系统镜像 (iso 标签)， 还是属于打包部署的其他标签。所以 Tag 是一种完美的结构管理

## Token

- 对于这种小型的仓库来说， 不需要天天盯着别人下载了什么， 因为本身的定位就是存储一些常用的静态文件分享给一些不方便科学上网的人， 或者一些常用的配置包和配置文件， 因而我们选择 极简的三种权限管理方式 (read install write 三种权限) + Token 认证方式， 传统的角色中心的权限设计在这里是冗余的， 只会增加理解成本。 我的目标是让用户像使用 `apt` 一样使用 `wt` ,只需要在最开始的时候配置相应的 token, 就不再需要进行调整。 再次强调， `wt` 的定位是分发和长期存储一些常用的包， 而不是进行版本管理或者其他东西， 引入递归上传也偏离了设计初衷, 因此 **将目录打包和压缩是上传的人的责任,而不是wt需要解决的问题**

> 以下内容是待开发的
>
## Public

- 因为有时候需要临时分享， 分享给一两个人还能手动配置token, 但是一但人数多起来， 就不太现实， 所以 `wt` 提供一种可以向所有的人开放 install 权限的方式， 只需要运行
`wt public <package name>` 就要将 这个package开放， 不需要token就能下载， 这在临时分享的场景是必要的也是实用的

## Discover

- 当前版本中， 需要手动添加仓库的服务器， 这也有点麻烦， 所以后期希望添加一个 Discover 功能对局域网进行广播， 然后自动把局域网中开启 `wt server` 的服务器加入到里面， 对于一些public包可以直接拉取而不需要进行任何配置， 这主要是用来提升体验的。 还有一点非常重要， 就是传统的仓库管理工具都是中心服务器的， 而 `wt` 一个二进制文件就可以同时实现 `server` 和 `client` 的功能， 可以想象一下这个场景: 你跟公司里面的其他人都连的是 公司内网， 然后都安装了 `wt`并启动了server, 想要下载一个东西， 可以通过discover将公司里其他人的设备都加入到你的 mirror 中， 在查找一个常用的包的时候， 你或许因为隐私或者其他问题不希望从其他地方下载， 这个时候就可以在所有人公开出来的仓库中查找， 这是一种完全的去中心化和集群的仓库集成策略， 你们公司的所有设备都是仓库的一部分， 各取所需, 提升了效率
